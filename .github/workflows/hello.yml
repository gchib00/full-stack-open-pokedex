name: Hello World!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: Current date
        run: |
          date
      - name: Current dir content
        run: |
          ls -lion that will lint the code. If the checks don't pass, Github Actions will show a red status.